# Cashout Multiplayer Prototype

## Local Run
1. `npm install`
2. `npm run install:all`
3. `npm run dev`

Open two browser tabs at the Vite URL to see multiplayer.

## Manual Run
Server:
1. `cd server`
2. `npm install`
3. `npm run dev`

Client:
1. `cd client`
2. `npm install`
3. `npm run dev`
